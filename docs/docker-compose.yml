services:
  mltk-docs:
    build:
      context: ..
      dockerfile: docs/Dockerfile
    container_name: mltk-docs
    ports:
      - "8080:80"
    restart: unless-stopped
    volumes:
      - nginx-logs:/var/log/nginx

volumes:
  nginx-logs:
