apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mltk.fullname" . }}
  labels:
    {{- include "mltk.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "mltk.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "mltk.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: mltk-server
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - server
            - --host
            - {{ .Values.server.host | quote }}
            - --port
            - {{ .Values.server.port | quote }}
            - --db
            - {{ .Values.server.dbPath | quote }}
          ports:
            - name: http
              containerPort: {{ .Values.server.port }}
          env:
            - name: MLTK_DB_PATH
              value: {{ .Values.server.dbPath | quote }}
          volumeMounts:
            - name: data
              mountPath: /data
          livenessProbe:
            httpGet:
              path: /api/health/live
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /api/health/ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: data
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "mltk.fullname" . }}-data
          {{- else }}
          emptyDir: {}
          {{- end }}
