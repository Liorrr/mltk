apiVersion: v1
kind: Service
metadata:
  name: {{ include "mltk.fullname" . }}
  labels:
    {{- include "mltk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "mltk.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
